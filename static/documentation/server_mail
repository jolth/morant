
** Servidor Smarthost

Install Exim:

$ sudo apt install exim4


Reconfigurar SmartHost:

$ sudo dpkg-reconfigure exim4-config

#images


Configure the account:

$ sudo vim /etc/exim4/passwd.client


Restart:

$ sudo systemctl restart exim4.service

